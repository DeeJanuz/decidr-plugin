# Unreleased

- **fix**: Update DecidR startup rules so lightweight internal research, preflight, verification, correction, and internal-task notes use audit breadcrumbs instead of catch-up decisions.
