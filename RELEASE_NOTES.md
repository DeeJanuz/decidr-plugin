# Unreleased

- Add lean `recentAuditEvents` init breadcrumbs for DecidR sessions.
- Add `log_governance_breadcrumb` for research, preflight, verification, and correction notes.
- Inject explicit MCPViews project context into DecidR init calls when configured.
