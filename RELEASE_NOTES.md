# Unreleased

- Rebuilt the DecidR dashboard around one authenticated `dashboard.v1` summary and one bounded background drilldown request.
- Added a stable loading shell, secure 60-second in-memory previews, request cancellation, and strict organization isolation.
- Removed dashboard row-by-row GitHub loading and added server-backed Next Steps filtering with deterministic cursor pagination.
- Added bundle, response-size, request-shape, and staging performance gates.
