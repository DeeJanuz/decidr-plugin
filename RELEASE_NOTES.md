# Unreleased

- **fix**: Honor an explicit pushed `organization_id` when Apps menu launches a DecidR renderer, even if the shared API client has already auto-bound a default organization.
- **fix**: Hide superseded decisions from dashboard pending-work surfaces.
- **fix**: Remove colored left accent rails from dashboard project cards and related card surfaces, including graph project cards, slideout task cards, superseded decision rows, and timeline project group/overflow treatments.
