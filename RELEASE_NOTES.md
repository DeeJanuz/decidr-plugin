# Unreleased

- **fix**: Preserve full MCPViews `rich_content` plan artifacts when agents save DecidR PLAN document versions from seeded startup rules, instead of saving condensed summaries.
- **fix**: Bump DecidR lifecycle and work-logging startup rule versions so installed agent-native rules can reconcile the full-plan artifact guidance.
